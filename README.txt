/* AlumiNect */

The Oracle database tables can be created by running createTables.php
NOTE: this php script will create a user as well.

The superadmin credentials will be entered in the database when the tables are created....hence the superadmin will have to approve himself when he registers as an admin else he will have no access to the homepage.
This can be don by visiting  superLogin.php.
Default username is mac.
default password is m1234.
